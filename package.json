{
  "name": "fuelcert-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -H 127.0.0.1",
    "build": "next build",
    "start": "next start -H 127.0.0.1",
    "clean": "rm -rf .next",
    "dev:clean": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true; rm -rf .next && next dev -H 127.0.0.1",
    "preview": "npm run build && next start -H 127.0.0.1"
  },
  "dependencies": {
    "next": "15.3.9",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
